# Project setup

This is a Vite, React, and TypeScript web application.

## Run

Use the **Start application** workflow. It runs:

```sh
npm run dev
```

The development server listens on `0.0.0.0:5000` for the Replit web preview.

## Checks

```sh
npm run lint
npm run build
```